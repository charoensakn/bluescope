{
  "name": "@repo/modules",
  "private": true,
  "version": "0.1.1",
  "exports": {
    ".": {
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf ./dist",
    "lint": "biome lint  --write",
    "format": "biome format --write",
    "check": "biome check --write",
    "test": "vitest run src"
  },
  "dependencies": {
    "@repo/agents": "*",
    "@repo/repos": "*"
  }
}
